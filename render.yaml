services:
  - type: web
    name: greenhouse-sensor-api
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: bash start.sh
    envVars:
      - key: DATABASE_URL
        value: sqlite:///./greenhouse.db

